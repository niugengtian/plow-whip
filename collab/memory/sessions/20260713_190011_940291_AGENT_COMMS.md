# Archived: AGENT_COMMS.md
**Project:** plow-whip
**Archived:** 2026-07-13T19:00:11
**Trigger:** block-rotation (96 lines, 2 blocks → keep 1)

## Carry Forward
- Archived messages: 1
- [system] 2026-07-13T18:18:30

## Archived Messages

### [system] 2026-07-13T18:18:30

task T-20260713181253 complete: ## 复审结论

- 测试: `python3 -m unittest discover -s tests -q` → 130 tests, OK (exit 0)
- 保护路径未修改: `git diff --exit-code -- plow_whip tests README.md pyproject.toml` → clean
- 审查范围: plow_whip/ 全模块、tests/、CLI 入口 agent_flow.py、协议/路由/调度/dispatch 关键路径

## 可操作发现（按严重级别）

### 高

**H1 — 验收命令经 shell 执行，存在注入与跨平台脆弱性**
- 位置: `plow_whip/agent_flow.py:1645-1648`
- 触发: `task complete` 且 `verify_commands` 含 `;`、`|`、`&`、未转义路径或 Windows 专用语法
- 影响: 可能执行非预期 shell 片段；含特殊字符的合法命令在 Windows 上误失败，导致任务无法完成
- 最小修复: 将 `shell=True` 改为 `shlex.split(command)` + `shell=False`；无法拆分时显式拒绝并记录错误

### 中

**M1 — AGENT_COMMS 追加无文件锁**
- 位置: `plow_whip/agent_flow.py:417-418`
- 触发: 并发 `task progress` / `handoff` / `append_comms` 同时写入同一项目
- 影响: 消息块交错或截断，Warm 层定向消息损坏
- 最小修复: 对 `comms_file(project)+".lock"` 使用现有 `file_lock`，在锁内 read-modify-write 或单行原子追加

**M2 — dispatch 执行审计写入无 revision 重试**
- 位置: `plow_whip/dispatch.py:705-726`
- 触发: CLI 完成回写 `execution` 与子 Agent 同时 `task progress` 更新状态
- 影响: `RuntimeError` 被吞掉后 execution 证据丢失，调度器追责链断裂
- 最小修复: 仿 `_record_cli_session` 增加最多 3 次 load-retry-save 循环

**M3 — qoder_session 轮转非原子**
- 位置: `plow_whip/qoder_session.py:231-237`
- 触发: `rotate_session` 在写归档后、重写活跃文件前进程崩溃
- 影响: 活跃 JSONL 可能变空或重复，会话历史损坏
- 最小修复: 先写临时文件再 `os.replace`；或先备份活跃文件到 by_rm

**M4 — permissions.json 全局状态无锁**
- 位置: `plow_whip/dispatch.py:60-117`
- 触发: 并行 dispatch 调用 `check_permission` / `set_permission`
- 影响: `allow_remaining`/`ask_remaining` 计数错乱
- 最小修复: 对 `~/.plow-whip/permissions.json.lock` 使用 `file_lock` 包裹读写

**M5 — CLI 成功判定仅比较 task 对象快照**
- 位置: `plow_whip/dispatch.py:232-235`, `453-454`
- 触发: 子 Agent 只更新 `AGENT_COMMS` 或 legacy 顶层字段而未改 `task.*`
- 影响: CLI 返回码 0 仍被判为「任务状态未推进」，触发无谓 failover/重试
- 最小修复: 比较 `revision` 或 `updated_at`，或文档强制经 `task progress` 回写

### 低

**L1 — qoder_session / brain 零单测**
- 位置: `plow_whip/qoder_session.py`（全文）、`plow_whip/brain.py`（全文）
- 触发: 回归时仅依赖手工验证
- 影响: 轮转/回退/复杂度路由/DeepSeek 调用无自动化保障
- 最小修复: 为 `rotate_session`、`rollback`、`classify_complexity` 增加临时目录 fixture 测试

**L2 — load_state 失败直接 sys.exit**
- 位置: `plow_whip/agent_flow.py:326-327`
- 触发: 库式 import 后项目不存在
- 影响: 无法被上层捕获，不利于 scheduler/测试复用
- 最小修复: 抛出 `FileNotFoundError`/`ProjectNotFoundError`，CLI 层统一 exit

**L3 — file_lock 30s 陈旧锁可抢占**
- 位置: `plow_whip/io_utils.py:58-60`
- 触发: 持锁操作异常挂起 >30s（NFS/慢盘）
- 影响: 第二写入者抢锁，可能造成 revision 冲突或双写
- 最小修复: 锁目录内写 holder pid+心跳，仅当 pid 不存活才清除

## 残余风险（未在本次发现为缺陷）

- CI 未对接真实 `cursor-agent`/`codex` 二进制，CLI 流式解析仅 mock 覆盖
- scheduler 仅 render/dry-run 单测，Windows Task Scheduler / Linux systemd 无集成测试
- 多 Agent 同时写 inbox/state 依赖 revision+有限重试，极端并发下仍可能需人工 repair
- Brain/notify/zellij 通道为 macOS/可选依赖，Linux/Windows 无人值守路径主要是 file inbox

## 总体评价

核心路径（协议 v4、Registry/Router/Driver、原子 task 状态、revision 乐观锁、whip probe 边界、CLI session 绑定）设计一致且测试覆盖较好；上述 H1/M1-M5 为可定位、可小步修复的问题，不构成架构性阻断。
